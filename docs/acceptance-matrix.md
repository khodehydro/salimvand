# ماتریس پذیرش Release Candidate

این ماتریس معیارهای تحویل را از منطق معماری و مرجع بصری به checkهای قابل اجرا تبدیل می‌کند.

## وضعیت کد و داده

| معیار                              | وضعیت | روش بررسی                                   |
| ---------------------------------- | ----- | ------------------------------------------- |
| TypeScript strict                  | آماده | `pnpm typecheck`                            |
| پول به‌صورت Integer ریال           | آماده | rules و DTOهای Invoice/Purchase/Payment     |
| زمان UTC                           | آماده | Prisma schema و runtime config              |
| فروش اتمیک و Ledger                | آماده | تست InvoiceService و PostgreSQL integration |
| پرداخت و مرجوعی                    | آماده | تست‌های Invoice و Customer                  |
| Soft Delete                        | آماده | Customer/Supplier service                   |
| Audit عملیات حساس                  | آماده | سرویس‌های فروش، خرید، تنظیمات و موجودی      |
| public serializer بدون قیمت و قفسه | آماده | تست public invoice و public catalog         |

## عملیات و استقرار

| معیار                        | وضعیت         | روش بررسی                                                 |
| ---------------------------- | ------------- | --------------------------------------------------------- |
| Prisma generate/validate     | روی CI/VPS    | `scripts/release-candidate-check.sh`                      |
| تست API و shared             | آماده         | ۱۹۰ تست API + ۱۲ shared + ۷ ui + ۲۷ admin (مجموع ۲۳۶ تست) |
| Build API/Website/Admin      | آماده         | Release candidate script و Next/Vite build                |
| PostgreSQL و Redis readiness | آماده         | `/api/v1/health/ready`                                    |
| Queue retry و shutdown       | آماده         | تست Notification service                                  |
| Backup manifest/checksum     | آماده         | `verify-backup.sh` و لاگ‌های backup_jobs                  |
| Restore جداگانه و guarded    | آماده         | `restore.sh` و dry-run                                    |
| GitHub Actions CI/CD         | آماده         | `.github/workflows/ci.yml` و `deploy.yml`                 |
| Docker integration test      | آماده برای CI | `pnpm integration:check`                                  |

## رابط کاربری

| معیار                                       | وضعیت          | توضیح                                                                                                                                                                                                                                                          |
| ------------------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RTL و responsive shell                      | پیاده‌سازی شده | نیازمند بازبینی مرورگری نهایی                                                                                                                                                                                                                                  |
| Light/Dark                                  | پیاده‌سازی شده | نیازمند بررسی همهٔ صفحات                                                                                                                                                                                                                                       |
| سایت کاتالوگی بدون قیمت عمومی               | آماده          | CTA استعلام و عدم وجود cart/payment                                                                                                                                                                                                                            |
| ثبت یکپارچهٔ محصول (انبار = کاتالوگ = سایت) | پیاده‌سازی شده | پنجرهٔ «ثبت محصول» در تب انبار: مشخصات + قلم انبار + سازگاری، ذخیرهٔ ثابت در پایین پنجره                                                                                                                                                                       |
| اصلاح لحظه‌ای موجودی از لیست                | پیاده‌سازی شده | استپر −/+ کنار عدد موجودی در لیست محصولات (هر برند جدا) و لیست انبار؛ حذف فرم اصلاح موجودی                                                                                                                                                                     |
| انبار تب‌بندی‌شده                           | پیاده‌سازی شده | تب‌های ثبت محصول / لیست انبار (جست‌وجو + بارکدخوان) / قفسه‌ها                                                                                                                                                                                                  |
| جست‌وجوی لحظه‌ای (بدون دکمه)                | پیاده‌سازی شده | فیلد جست‌وجوی جدید محصولات و لیست انبار: آیکن + پاک‌کردن ✕؛ انبار با debounce ۳۰۰ms همان لحظه بارگذاری می‌شود، بدون دکمهٔ جست‌وجو                                                                                                                              |
| لیست انبار گروه‌بندی per محصول              | پیاده‌سازی شده | هر محصول: تصویر + نام + چیپ «۲ قلم · ۷ قطعه» + ارزان‌ترین قیمت + ردیف هر قلم (برند، بارکد، استپر، قفسه)؛ API تصویر اصلی محصول را هم برمی‌گرداند                                                                                                                |
| بارکد به‌صورت تصویر قابل اسکن               | پیاده‌سازی شده | jsbarcode: EAN13 برای بارکد ۱۳رقمی، وگرنه CODE128؛ در کارت قلم و پاپ‌آپ چاپ لیبل (SVG سریال‌شده) — قابل خواندن با بارکدخوان از روی صفحه                                                                                                                        |
| لینک کوتاه امن فاکتور در پنل                | پیاده‌سازی شده | دکمهٔ «لینک» در لیست فاکتورها ← دیالوگ: لینک کوتاه توکن‌دار /i/<کد> + کپی + QR + بازکردن + صدور لینک جدید؛ شمارهٔ فاکتور در لینک نیست، لینک قبلی باطل و اعتبار ۳۰ روز                                                                                          |
| دامنهٔ لینک عمومی فاکتور                    | پیاده‌سازی شده | لینک همیشه به فروشگاه عمومی (salimvand.ir) می‌رود نه به پنل (cms.*)؛ VITE_PUBLIC_SITE_URL از PUBLIC_SITE_URL در deploy.sh داخل باندل پنل تزریق می‌شود و بدون آن زیردامنهٔ cms حذف می‌شود؛ فاکتور بدون لاگین نمایش داده می‌شود (API عمومی + صفحهٔ /i/<کد> سایت) |
| تب‌بندی صدور/آرشیو فاکتورها                 | پیاده‌سازی شده | صفحهٔ فاکتورها دو تب دارد: «صدور فاکتور» و «فاکتورهای صادر شده» با جست‌وجوی لحظه‌ای شماره/نام/موبایل و فیلتر وضعیت پرداخت                                                                                                                                      |

| آدرس فروشگاه و مشتری روی فاکتور | پیاده‌سازی شده | فرم صدور: آدرس فروشگاه (پیش‌فرض از تنظیمات) + آدرس مشتری (پیش‌فرض از پروندهٔ مشتری)؛ روی فاکتور صادرشده نیز قابل ویرایش (PATCH /invoices/:id/addresses)؛ چاپ در PDF و نمایش در فاکتور عمومی |

| برگشت جزیی اقلام فاکتور | پیاده‌سازی شده | از جزئیات فاکتور، هر قلم با تعداد دلخواه (۱ از ۳) برگشت می‌خورد: مبلغ فاکتور کم می‌شود (netTotal)، قطعهٔ سالم به دارایی انبار برمی‌گردد و تراکنش return ثبت می‌شود؛ پرداخت‌های جدید روی مبلغ خالص سقف می‌خورند |

| برگشت قطعهٔ خراب بدون ورود به انبار | پیاده‌سازی شده | سوییچ «قطعه سالم است» خاموش ← restock=false: موجودی انبار زیاد نمی‌شود، فقط مبلغ فاکتور و بدهی کم می‌شود؛ در تاریخچهٔ برگشتی‌ها با برچسب «خراب» مشخص است |
| ثبت پرداخت فاکتور (رفع ۵۰۰) | پیاده‌سازی شده | ریشهٔ خطای سرور: مهاجرت reconcile پیش‌فرض id جدول‌ها را حذف کرده بود ولی INSERT خام payments بدون id بود؛ هر سه INSERT خام (payments + دو upsert مشتری) با gen_random_uuid() صریح اصلاح و روی دیتابیس واقعی تست شد |

| فونت فارسی PDF (رفع خروجی خراب) | پیاده‌سازی شده | فونت Vazirmatn داخل ریپو باندل شد + شکل‌دهی حروف (presentation forms) و الگوریتم bidi برای pdfkit؛ متن فارسی متصل و راست‌به‌چپ، اعداد و کدهای لاتین سالم؛ تست واحد برای فاکتور PDF با فونت جاسازی‌شده |

| تماس و آدرس فروشگاه از تنظیمات روی فاکتور | پیاده‌سازی شده | سرور هنگام صدور فاکتور آدرس و شماره تماس فروشگاه را از store.profile تنظیمات اسنپ‌شات می‌کند؛ فرم صدور دیگر چیزی از فروشگاه نمی‌گیرد (فقط نمایش فقط-خواندنی)؛ فاکتورهای قدیمی هم هنگام نمایش/PDF از تنظیمات پر می‌شوند |

| شماره تماس مشتری و فروشگاه در فاکتور | پیاده‌سازی شده | تماس مشتری و فروشگاه در مودال جزئیات پنل، فاکتور عمومی سایت و PDF نمایش داده می‌شود؛ تماس فروشگاه با ستون storePhone (مهاجرت) |

| داشبورد مطابق داکیومنت | پیاده‌سازی شده | چیدمان دقیق mock داکیومنت: سربرگ + بازهٔ زمانی سگمنتی (امروز/۷/۳۰/۹۰ روز) + ۴ کارت KPI (فروش امروز با درصد دیروز، فاکتورهای امروز، مشتریان بدهکار، هشدار کمبود قرمز) + ردیف نمودار میله‌ای فروش/دوناتِ ترکیب موجودی بر اساس دسته + کارت بدهکارها و کمبود موجودی (stockbar) + تایم‌لاین تراکنش‌های انبار + کارت سلامت یکپارچه‌سازی‌ها؛ summary API فیلد categoryComposition را هم برمی‌گرداند |

| فوتر فاکتور عمومی مطابق داکیومنت | پیاده‌سازی شده | بند برند تیره بالای صفحه + کارت متای هم‌پوشان (شماره فاکتور، متادیتا، QR) + کارت مستقل «اقلام فاکتور» (ستون # و چیپ برند) + فوتر دو ستونه: کارت‌های پرداخت و شرایط و یادداشت بدهی سمت راستِ چیدمان، کارت تیرهٔ جمع اقلام/تخفیف/قابل پرداخت/پرداخت‌شده/باقی‌مانده سمت دیگر؛ ردیف پایانی دانلود PDF/چاپ/کپی لینک |

| صفحه صدور فاکتور مطابق داکیومنت | پیاده‌سازی شده | چیدمان mock داکیومنت: نوار مشتری (چیپ مشتری انتخاب‌شده با آواتار/بدهی/تعداد فاکتور یا فیلدهای مشتری حضوری + جست‌وجوی لحظه‌ای)، باکس اسکن (جست‌وجو/بارکد + دوربین + نتیجه‌های گروه‌بندی‌شده per محصول با دکمهٔ هر برند و قیمت و قفسه)، ردیف‌های اقلام با استپر تعداد و فی و تخفیف قابل‌ویرایش و جمع لحظه‌ای، پنل مبالغ چسبان (جمع اقلام، تخفیف ردیف‌ها، تخفیف کل، مبلغ نهایی درشت، باکس پرداخت با چک‌باکس نقدی/کارت‌خوان/واریز/نسیه + تسویه کامل، یادداشت بدهی) و دکمهٔ درشت صدور؛ مودال موفقیت و مودال ثبت پرداخت فاکتور هم به سبک کارت بازطراحی شد |

| رفع ۵۰۰ ثبت پرداخت مشتری | پیاده‌سازی شده | پاسخ POST /customers/:id/payments دیگر BigInt خام برنمی‌گرداند (amount و remainingDebt به رشتهٔ JSON-safe تبدیل شدند؛ list/get/debtors هم همین‌طور) و actorId خالی به‌جای خطای FK مبهم، ۴۰۰ با پیام روشن می‌دهد؛ تست رگرسیون JSON.stringify بدون replacer اضافه شد |
| رفع ۵۰۰ ثبت پرداخت فاکتور | پیاده‌سازی شده | سه ریشهٔ همزمان رفع شد: (۱) درج پرداخت با INSERT خام به gen_random_uuid() وابسته بود — حالا tx.payment.create پرسمای؛ (۲) پیامک رسید داخل تراکنش await می‌شد و قطعی Redis کل پرداخت را ۵۰۰ می‌کرد — به بعد از تراکنش منتقل شد و enqueue دیگر هرگز throw نمی‌کند؛ (۳) upsert مشتری هم به Prisma upsert تبدیل شد |

| ویرایش مشتری + تب‌بندی صفحهٔ مشتریان | پیاده‌سازی شده | صفحهٔ مشتریان دو تب شد (لیست مشتریان / ثبت مشتری جدید)؛ جست‌وجوی لحظه‌ای بدون دکمه؛ ویرایش کامل اطلاعات مشتری (نام/موبایل/آدرس/یادداشت) از ردیف و از پرونده؛ پروندهٔ مشتری به‌صورت مودال کارتی با فهرست فاکتورها (شماره/تاریخ/مبلغ/وضعیت + لینک به جزئیات فاکتور) و خودروها |

| رفع ذخیره‌نشدن تب اقلام محصول | پیاده‌سازی شده | پیام‌ها حالا داخل مودال نمایش داده می‌شوند (قبلاً پشت بک‌دراپ نامرئی بودند و خطا شبیه «بی‌اثر بودن دکمه» بود)؛ بارکد تکراری و برند تکراری برای محصول به‌جای ۵۰۰، پیام فارسی روشن می‌دهند؛ دکمهٔ پایین تب اقلام حالا ویرایش‌های ردیفی را هم ذخیره می‌کند و بعد قلم جدید را ثبت می‌کند |

| کارت حافظهٔ سرور در داشبورد | پیاده‌سازی شده | GET /dashboard/system (فقط مدیر): مصرف/کل حافظهٔ RAM و فضای دیسک با نوار پیشرفت رنگی و درصد، در کارت «یکپارچه‌سازی‌ها و سلامت سیستم» |

| رفع صفحهٔ سفید گزارش‌ها | پیاده‌سازی شده | ریشه: نمودار فروش جدید تاریخ‌های ازپیش‌فرمت‌شدهٔ شمسی گزارش‌ها را به new Date() می‌داد و کرش می‌کرد؛ اکنون برچسب‌های غیرتاریخی پاس می‌شوند + ErrorBoundary سراسری صفحه‌ها که هر کرش رندر را به پیام خوانا تبدیل می‌کند نه صفحهٔ سفید |

| رفع ۵۰۰ ثبت پرداخت مشتری | پیاده‌سازی شده | پاسخ POST /customers/:id/payments دیگر BigInt خام برنمی‌گرداند (amount و remainingDebt به رشتهٔ JSON-safe تبدیل شدند؛ list/get/debtors هم همین‌طور) و actorId خالی به‌جای خطای FK مبهم، ۴۰۰ با پیام روشن می‌دهد؛ تست رگرسیون JSON.stringify بدون replacer اضافه شد |بند برند تیره بالای صفحه + کارت متای هم‌پوشان (شماره فاکتور، متادیتا، QR) + کارت مستقل «اقلام فاکتور» (ستون # و چیپ برند) + فوتر دو ستونه: کارت‌های پرداخت و شرایط و یادداشت بدهی سمت راستِ چیدمان، کارت تیرهٔ جمع اقلام/تخفیف/قابل پرداخت/پرداخت‌شده/باقی‌مانده سمت دیگر؛ ردیف پایانی دانلود PDF/چاپ/کپی لینک |

| ویرایش نام نمایشی کاربران | پیاده‌سازی شده | تنظیمات ← کاربران ← «ویرایش»: نام نمایشی + نقش + رمز جدید (PATCH /users/:id با name) |

| سوییچ نمایش قیمت در سایت | پیاده‌سازی شده | تنظیمات ← «قیمت‌ها در سایت» (سوییچ سراسری) + بازنویسی تک‌محصولی (مطابق سایت / همیشه نمایش / همیشه پنهان) در تب «پایه و سئو»؛ قیمت فقط وقتی مجاز باشد در پاسخ عمومی می‌آید |
| کتابخانهٔ رسانه + انتخاب از رسانه‌های موجود | پیاده‌سازی شده | پنل: صفحهٔ رسانه‌ها، انتخاب لوگو/فاوآیکون/تصویر محصول از کتابخانه |
| پیش‌نمایش رسانه‌ها در پنل | پیاده‌سازی شده | سرو `/uploads` روی API و Nginx پنل (درج خودکار توسط deploy.sh) |
| صفحات SEO محصول/دسته/خودرو/موقعیت | آماده | metadata، canonical و JSON-LD |
| CMS و command palette | پیاده‌سازی شده | نیازمند تست تعاملی مرورگر |
| انبار، فروش، خرید و تنظیمات | پیاده‌سازی شده | نیازمند acceptance دستی در پنل |
| تست UI/E2E واقعی | آماده در CI | `pnpm test:e2e` با Playwright پس از build، چون سرویس‌های سایت/پنل بالا می‌آیند |
| شکل‌دهی کامل متن فارسی PDF (برچسب‌ها + مقادیر) | انجام شده | تست رگرسیون با monkey-patch روی `PDFDocument.text`؛ هیچ خط draw شده نباید حروف پایهٔ فارسی داشته باشد |
| سطر «باقی‌مانده (بدهی)» و وضعیت فارسی پرداخت در PDF | انجام شده | پس از برگشتی، PDF مبلغ خالص + باقی‌مانده را نشان می‌دهد |
| تسویه خودکار وضعیت پرداخت پس از برگشتی | انجام شده | بدهی ۵۰هزار + برگشتی ۵۰هزار ← وضعیت «تسویه شده» و بدهی صفر (تست واحد) |
| مانده و مبلغ فاکتور در مودال پرداخت بر مبنای خالص | انجام شده | `net(invoice) − paidAmount` به‌جای `total − paidAmount` |
| پیشنهاد خودکار مشتری هنگام تایپ نام/موبایل در صدور فاکتور | انجام شده | autocomplete روی خود ورودی‌ها با debounce؛ نبود تطابق ← صدور حضوری |
| چیدمان بصری نهایی PDF (بدون آینه‌شدن کلمات) | انجام شده | رندر واقعی با MuPDF و تطبیق مختصات تک‌تک حروف ۲۰ خط با خروجی faText؛ direction فونت در renderPdf روی ltr فورس می‌شود |
| بدهی مشتری در همه‌جا بر مبنای خالص پس از برگشتی | انجام شده | calculateCustomerDebt در لیست/جزئیات/debtors مشتری، پرداخت مشتری و گزارش‌ها refundAmount ها را کم می‌کند |
| صفحهٔ «برچسب محصولات» مطابق داکیومنت more features | انجام شده | سه اندازه (۵۰×۳۰/۶۰×۴۰/۳۸×۲۲)، سه سبک (برند/تک‌رنگ/ناوی)، EAN-13 و Code 128 برداری، برگهٔ A4 با حاشیهٔ ۸mm و فاصلهٔ ۲mm؛ ۱۲ تست واحد + endpoint «inventory/labels» |
| یک‌کلیکی بودن برچسب از ردیف انبار | انجام شده | دکمهٔ «برچسب» هر قلم انبار ← استودیوی برچسب با همان کالا preselect شده (#/labels?item=…) |
| بازبینی رنگ‌های خام و Format کل repository | انجام شده | `pnpm format:check` سبز + `.prettierignore` برای مرجع بصری و lockfile |

## اجرای نهایی

```bash
bash scripts/release-candidate-check.sh
```

این فرمان قبل از Release، syntax اسکریپت‌ها، Prisma Client، schema validation، typecheck، test و build را اجرا می‌کند. اجرای کامل آن به دسترسی Prisma Engine نیاز دارد. تست‌های Docker، Restore واقعی و بازبینی بصری باید روی CI/VPS و مرورگر واقعی انجام شوند.

تا وقتی ردیف‌های «باقی‌مانده» انجام نشده‌اند، وضعیت پروژه از نظر acceptance بصری ۱۰۰٪ اعلام نمی‌شود؛ هستهٔ عملیاتی و backend قابل Deploy است، اما تحویل نهایی نیازمند تأیید دستی UI و اجرای integration واقعی است.
