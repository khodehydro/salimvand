/** Shared handle on the framework metadata keys so tests assert against the
 * real NestJS constants instead of copies of them. */
export { PATH_METADATA, METHOD_METADATA, INTERCEPTORS_METADATA } from '@nestjs/common/constants';
