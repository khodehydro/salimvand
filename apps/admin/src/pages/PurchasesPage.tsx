import { useEffect, useState } from 'react';
import { api } from '../lib/api';

type Supplier = { id: string; name: string };
type Item = { id: string; barcode: string; product: { name: string; code: string }; brand: { name: string }; quantity: number };
type Line = { inventoryItemId: string; productName: string; quantity: number; unitPrice: string };
type Purchase = { id: string; number: string; supplierName: string; total: string | number; paidAmount: string | number; issuedAt: string };
export function PurchasesPage() { const [suppliers, setSuppliers] = useState<Supplier[]>([]); const [items, setItems] = useState<Item[]>([]); const [purchases, setPurchases] = useState<Purchase[]>([]); const [supplierId, setSupplierId] = useState(''); const [itemId, setItemId] = useState(''); const [quantity, setQuantity] = useState('1'); const [unitPrice, setUnitPrice] = useState(''); const [lines, setLines] = useState<Line[]>([]); const [paidAmount, setPaidAmount] = useState('0'); const [message, setMessage] = useState(''); useEffect(() => { void api<{ data: Supplier[] }>('/suppliers').then((r) => setSuppliers(r.data)).catch((e: Error) => setMessage(e.message)); void api<{ data: Item[] }>('/inventory').then((r) => setItems(r.data)).catch((e: Error) => setMessage(e.message)); void api<{ data: Purchase[] }>('/purchases').then((r) => setPurchases(r.data)).catch((e: Error) => setMessage(e.message)); }, []); const addLine = () => { const item = items.find((row) => row.id === itemId); const count = Number(quantity); if (!item || !Number.isInteger(count) || count <= 0 || !/^[0-9]+$/.test(unitPrice)) return setMessage('قلم، تعداد و قیمت خرید معتبر الزامی است'); setLines([...lines, { inventoryItemId: item.id, productName: `${item.product.name} · ${item.brand.name}`, quantity: count, unitPrice }]); setItemId(''); setQuantity('1'); setUnitPrice(''); }; const total = lines.reduce((sum, line) => sum + line.quantity * Number(line.unitPrice), 0); const submit = async () => { if (!supplierId || !lines.length) return setMessage('تأمین‌کننده و حداقل یک قلم خرید الزامی است'); try { await api('/purchases', { method: 'POST', body: JSON.stringify({ supplierId, paidAmount, lines }) }); setMessage('فاکتور خرید ثبت شد و موجودی افزایش یافت'); setLines([]); setPaidAmount('0'); void api<{ data: Purchase[] }>('/purchases').then((r) => setPurchases(r.data)); } catch (error) { setMessage((error as Error).message); } }; return <section><div className="page-title"><div><span className="eyebrow">خرید</span><h1>فاکتورهای خرید</h1><p className="muted">ثبت خرید و افزایش اتمیک موجودی</p></div></div>{message && <div className="notice">{message}</div>}<div className="product-form customer-form"><label>تأمین‌کننده<select value={supplierId} onChange={(e) => setSupplierId(e.target.value)}><option value="">انتخاب تأمین‌کننده</option>{suppliers.map((row) => <option key={row.id} value={row.id}>{row.name}</option>)}</select></label><label>قلم انبار<select value={itemId} onChange={(e) => setItemId(e.target.value)}><option value="">انتخاب کالا</option>{items.map((row) => <option key={row.id} value={row.id}>{row.product.name} · {row.brand.name} · {row.barcode}</option>)}</select></label><label>تعداد<input type="number" min="1" step="1" value={quantity} onChange={(e) => setQuantity(e.target.value)} /></label><label>قیمت واحد ریال<input dir="ltr" inputMode="numeric" value={unitPrice} onChange={(e) => setUnitPrice(e.target.value)} /></label><button type="button" onClick={addLine}>افزودن قلم</button></div><div className="product-table"><div className="table-head customer-head"><span>کالا</span><span>تعداد</span><span>قیمت واحد</span><span>جمع</span><span>عملیات</span></div>{lines.map((line, index) => <div className="table-row customer-row" key={`${line.inventoryItemId}-${index}`}><strong>{line.productName}</strong><span>{line.quantity}</span><code>{line.unitPrice}</code><code>{line.quantity * Number(line.unitPrice)}</code><button className="row-action" onClick={() => setLines(lines.filter((_, rowIndex) => rowIndex !== index))}>حذف</button></div>)}</div>{lines.length > 0 && <div className="adjust-box"><strong>جمع فاکتور: {total.toLocaleString('fa-IR')} ریال</strong><label>پرداخت‌شده<input dir="ltr" inputMode="numeric" value={paidAmount} onChange={(e) => setPaidAmount(e.target.value)} /></label><button className="button-primary" onClick={() => void submit()}>ثبت فاکتور و افزایش موجودی</button></div>}<h2>فاکتورهای ثبت‌شده</h2><div className="product-table"><div className="table-head customer-head"><span>شماره</span><span>تأمین‌کننده</span><span>مبلغ</span><span>پرداخت</span><span>تاریخ</span></div>{purchases.map((purchase) => <div className="table-row customer-row" key={purchase.id}><code>{purchase.number}</code><strong>{purchase.supplierName}</strong><span>{Number(purchase.total).toLocaleString('fa-IR')} ریال</span><span>{Number(purchase.paidAmount).toLocaleString('fa-IR')} ریال</span><small>{new Date(purchase.issuedAt).toLocaleDateString('fa-IR')}</small></div>)}</div></section>; }
